<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!--
      Launch theme. Uses the modern Android 12+ SplashScreen API so we don't
      get a "double splash" (system default then ours). Attributes:
        windowSplashScreenBackground     — solid colour behind everything
        windowSplashScreenAnimatedIcon   — app icon, centered in safe area
        postSplashScreenTheme            — theme swapped in after splash fades

      The legacy splash.png is generated by play-store/generate-splash.py
      from assets/icon.png and serves two roles: (1) windowBackground for
      pre-API-31 devices that don't have the SplashScreen API, and (2) the
      Capacitor splash drawable that paints between the system splash
      handoff and the first WebView frame on all versions — that's where
      the icon + DayFlow wordmark composition lives, since the system
      splash API itself doesn't let us position a wordmark next to the
      icon (Branding asset is forced to the bottom edge of the screen).
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:windowBackground">@drawable/splash</item>
    </style>
</resources>