<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!--
      Launch theme. Uses the modern Android 12+ SplashScreen API so we don't
      get a "double splash" (system default then ours). Attributes:
        windowSplashScreenBackground     — solid colour behind everything
        windowSplashScreenAnimatedIcon   — app icon, centered in safe area
        windowSplashScreenBrandingImage  — "DayFlow" wordmark at the bottom
        postSplashScreenTheme            — theme swapped in after splash fades

      Branding image and the legacy splash.png are generated by
      play-store/generate-splash.py from assets/icon.png. The legacy
      windowBackground = @drawable/splash is the fallback for very old Android
      (< API 31) where the SplashScreen API isn't available.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:windowBackground">@drawable/splash</item>
        <!-- windowSplashScreenBrandingImage is only available on API 31+ as a
             system attribute (the AndroidX core-splashscreen lib doesn't
             backport it). See values-v31/styles.xml for the override that
             attaches the "DayFlow" wordmark below the icon on Android 12+. -->
    </style>
</resources>